import { FiltrosArrayPipe } from './filtros-array.pipe';

describe('FiltrosArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
